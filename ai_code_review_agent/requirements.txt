# AI Code Review & Security Analysis Agent -- dependencies
# Version floors only (not exact pins) so `pip install -r requirements.txt`
# always resolves the latest compatible release. Tested on Python 3.11/3.12,
# Windows 10/11.

# --- UI ---------------------------------------------------------------
streamlit>=1.40.0

# --- Core config / data models ----------------------------------------
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-dotenv>=1.0.1

# --- LangChain / LangGraph (multi-agent orchestration, RAG, LLM calls) -
langchain>=1.3.0
langchain-core>=0.3.0
langgraph>=0.3.0
langchain-text-splitters>=0.3.0
langchain-community>=0.3.0

# LLM providers (install/keep whichever you plan to use via .env LLM_PROVIDER)
langchain-openai>=0.3.0
langchain-anthropic>=0.3.0
langchain-google-genai>=2.0.0

# Local/offline embeddings (default -- no API key required)
langchain-huggingface>=0.1.2
sentence-transformers>=3.3.0

# Vector store for the Secure Coding Knowledge Base
langchain-chroma>=0.2.0
chromadb>=0.5.20

# --- Document ingestion (OWASP guideline PDFs / Markdown) -------------
pypdf>=5.1.0

# --- Static analysis (Python) ------------------------------------------
pylint>=3.3.0
bandit>=1.8.0
radon>=6.0.1

# --- Static analysis (Java) ---------------------------------------------
javalang>=0.13.0
# Optional deeper Java scan: install PMD separately (https://pmd.github.io/)
# and ensure `pmd` is on PATH -- auto-detected, not required.

# --- Report export -------------------------------------------------------
reportlab>=4.2.5

# --- Testing ---------------------------------------------------------------
pytest>=8.3.0
